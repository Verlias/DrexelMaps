// JavaScript source code
class PriorityQueue {
    constructor(start, end) {
        this.start = start;     // Start Node
        this.end = end;         // End Node
        this.queue = [];        // Array of Nodes
    }
}